apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.routes.invoices.service }}
  namespace: {{ .Values.namespaces.application }}
  labels:
    app: invoice
spec:
  type: ClusterIP
  selector:
    app: invoice-microservice
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP