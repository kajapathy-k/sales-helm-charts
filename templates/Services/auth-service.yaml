apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.routes.auth.service }}
  namespace: {{ .Values.namespaces.application }}
  labels:
    app: auth
spec:
  type: ClusterIP
  selector:
    app: auth-microservice
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      