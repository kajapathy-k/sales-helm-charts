apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute 
metadata:
  name: {{ .Values.routes.orders.routeName }}
  namespace: {{ .Values.namespaces.application }}
spec:
  parentRefs:
  - name: {{ .Values.gateway.gatewayName }}
    namespace: {{ .Values.namespaces.gateway }}
  rules:
  - matches:
    - path: 
        type: PathPrefix
        value: {{ .Values.routes.orders.path }}
    backendRefs:
    - name: {{ .Values.routes.orders.service }}
      port: {{ .Values.routes.orders.port }}

